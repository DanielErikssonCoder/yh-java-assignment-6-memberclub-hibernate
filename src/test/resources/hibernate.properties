# Use H2 Driver for testing
jakarta.persistence.jdbc.driver=org.h2.Driver

# 'mem:testdb' means it runs in RAM. 'DB_CLOSE_DELAY=-1' keeps it alive during tests.
jakarta.persistence.jdbc.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
jakarta.persistence.jdbc.user=sa
jakarta.persistence.jdbc.password=

# Use H2 Dialect so Hibernate speaks "H2-language"
hibernate.dialect=org.hibernate.dialect.H2Dialect

# Show SQL in console so we can see what happens
hibernate.show_sql=true

# Auto-create tables when tests start and drop them when finished
hibernate.hbm2ddl.auto=create-drop